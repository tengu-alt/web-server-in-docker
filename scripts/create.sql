create table IF NOT EXISTS signed_users (
                              firstname varchar,
                              lastname varchar,
                              email varchar,
                              user_password varchar
)